{
  "name": "buildly-tests",
  "version": "1.0.0",
  "description": "Testes E2E para Buildly — Gestão de Equipes",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
